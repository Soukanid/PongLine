events {}

http {


  ssl_certificate /etc/nginx/ssl/nginx.crt;
  ssl_certificate_key /etc/nginx/ssl/nginx.key;
  server {
    
    listen 443 ssl;
    server_name localhost;


    # location /api/auth/verify {
    #   # to prevent external access
    #   internal;
    #   proxy_pass http://auth-service:3001/verify;
    #
    #   proxy_pass_request_body off;
    #   proxy_set_header Content-Length "";
    #   proxy_set_header X-Original-URI $request_uri;
    #
    # }
    #
    location /api/auth/ {
       proxy_pass http://auth-service:3001/;
       proxy_set_header Host $host;
       proxy_set_header X-Real-IP $remote_addr;
    }

    location /api/chat/ {

      # auth_request /api/auth/verify;
      #
      # auth_request_set $user_id $upstream_http_x_user_id;
      # auth_request_set $user_alias $upstream_http_x_user_alias;
      # auth_request_set $user_name $upstream_http_x_user_name;

      # proxy_set_header X-User-Id $user_id;
      # proxy_set_header X-User-Alias $user_alias;
      # proxy_set_header X-User-Name $user_name;

      proxy_pass http://chat-service:3002/;
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";
         
    }

    location /api/game/ {

      # auth_request /api/auth/verify;
      #
      # auth_request_set $user_id $upstream_http_x_user_id;
      # auth_request_set $user_alias $upstream_http_x_user_alias;
      # auth_request_set $user_name $upstream_http_x_user_name;
      #
      # proxy_set_header X-User-Id $user_id;
      # proxy_set_header X-User-Alias $user_alias;
      # proxy_set_header X-User-Name $user_name;
      #
      proxy_pass http://game-service:3003;
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";
         
    }

    location /api/tournament/ {

      # auth_request /api/auth/verify;

      # auth_request_set $user_id $upstream_http_x_user_id;
      # auth_request_set $user_alias $upstream_http_x_user_alias;
      # auth_request_set $user_name $upstream_http_x_user_name;
      #
      # proxy_set_header X-User-Id $user_id;
      # proxy_set_header X-User-Alias $user_alias;
      # proxy_set_header X-User-Name $user_name;
      #
      proxy_pass http://tournament-service:3004/;
      proxy_set_header Host $host;
         
    }

    location /api/user-management/ {

      # auth_request /api/auth/verify;

      # auth_request_set $user_id $upstream_http_x_user_id;
      # auth_request_set $user_alias $upstream_http_x_user_alias;
      # auth_request_set $user_name $upstream_http_x_user_name;
      #
      # proxy_set_header X-User-Id $user_id;
      # proxy_set_header X-User-Alias $user_alias;
      # proxy_set_header X-User-Name $user_name;

      proxy_pass http://user-management-service:3005/;
      proxy_set_header Host $host;
         
    }
    location / {
      proxy_pass http://frontend:5173;
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";

    }
  }
}

